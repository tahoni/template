# {{Project Name}}

## 🧾 Change Log

All notable changes to this project are documented in this file. *(reusable as-is)*

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/), and this project adheres to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html). *(reusable as-is)*

This file is a **template**. Every section below is either directly reusable as-is (marked *(reusable as-is)*) or a
scaffold to fill in for the specific project (marked *(fill in)*) — replace the bracketed guidance with the
project's own details and delete these template notes once done.

---

### Table of Contents

- [🧪 Unreleased](#-unreleased)

*(fill in)* Add one entry per released version below the line above, most recent first, moving the "← Current"
marker onto the newest — per `AGENTS.md`'s Release Checklist:

```
- [🧾 Version {{X.Y.Z}}](#-xyz---yyyy-mm-dd) ← Current
```

---

### 🧪 [Unreleased]

*(fill in, if applicable)* Every notable change on the current branch gets an entry here, nested under the matching
Keep a Changelog category heading and an `##### <Area>` sub-heading — per `AGENTS.md`'s Git Workflow Conventions for
the exact structure and bullet style. Only the categories that actually have entries appear; delete the rest of this
list once real entries exist:

```
#### ➕ Added

##### {{Area}}

- {{what changed and why}}

#### 🔄 Changed

#### 🐛 Fixed

#### ⚠️ Deprecated

#### 🗑️ Removed

#### 🔐 Security
```

*(fill in)* When cutting a release, rename this section `### 🧾 [X.Y.Z] - YYYY-MM-DD` (keeping only the categories
that have entries), then start a fresh, fully-empty `### 🧪 [Unreleased]` section above it — per `AGENTS.md`'s
Release Checklist.
